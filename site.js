const method=document.querySelector('#compare-method'),frame=document.querySelector('#compare-frame');function refresh(){const id=String(frame.value).padStart(3,'0');document.querySelector('#compare-pred').src=`assets/views/${method.value}_${id}.png`;document.querySelector('#compare-gt').src=`assets/views/GT_${id}.jpg`;document.querySelector('#compare-label').textContent=`${method.value} / KEYFRAME ${frame.value}`;}if(method&&frame){method.addEventListener('change',refresh);frame.addEventListener('change',refresh);refresh();}const modelSelect=document.querySelector('#model-select');if(modelSelect)modelSelect.addEventListener('change',()=>{document.querySelector('#scene-viewer').src=`assets/${modelSelect.value}_cutaway.glb`;});addEventListener('scroll',()=>{const length=document.documentElement.scrollHeight-innerHeight;document.querySelector('#progress').style.width=(length>0?100*scrollY/length:0)+'%';},{passive:true});
